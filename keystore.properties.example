# Copy to keystore.properties (git-ignored) to sign release builds locally.
# The .keystore file must live outside the repo.

storeFile=/absolute/path/to/binthere-release.keystore
storePassword=...
keyAlias=binthere
keyPassword=...
